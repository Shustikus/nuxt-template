# Публичные переменные (доступны в браузере через useRuntimeConfig().public)
NUXT_PUBLIC_SITE_URL=http://localhost:3000
NUXT_PUBLIC_API_BASE=/api

# Только сервер (не префикс PUBLIC)
# NUXT_API_SECRET=

# Docker Compose (docker-compose.yaml)
HOST=0.0.0.0
PORT=3000
STORYBOOK_PORT=6006
NODE_ENV=production
