{/* Introduction.mdx */}

import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Введение" />

# Nuxt template

Живая документация UI: дизайн-токены (CSS в `app/assets/styles/tokens/`), сторис в `app/docs/`, компоненты в
`app/components/`.

Токены подключаются через `app/assets/styles/main.css`.
